var bugsnagApiWrapper = require('../lib/index.js');
var assert = require('assert');

describe('bugsnag-api-wrapper', function() {
  it('should work', function() {
    assert(bugsnagApiWrapper);
  });
});
